<form method="post" action="{{ path('nouvelle_mesure_delete', {'id': nouvelle_mesure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nouvelle_mesure.id) }}">
    <button class="btn btn-outline-primary"><i class="fa fa-trash-o" aria-hidden="true"></i> Supprimer</button>
</form>
